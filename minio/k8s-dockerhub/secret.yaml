apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: minio-demo
type: Opaque
stringData:
  # WARNING: For production, use proper secret management (e.g., sealed secrets, external secrets operator)
  username: minioadmin
  password: minioadmin123
