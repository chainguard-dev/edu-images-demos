apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Alternative Kubernetes manifests using official Docker Hub images
# Use this if you don't have access to Chainguard registry
# Usage: kubectl apply -k k8s-dockerhub/

namespace: minio-demo

resources:
  - namespace.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml

# Optional: uncomment to include the test job
# - test-job.yaml
